/**
 * The BaseController to be extended for concrete controller.
 */

define(
  [
    'underscore',
    'backbone'
  ],
  function(_, Backbone) {

    var BaseController = {

    };

    return BaseController;
  }
);